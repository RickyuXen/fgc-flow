# Recommended: set only DB_PASSWORD (URI is built automatically; special chars are encoded)
DB_PASSWORD=your_atlas_password

# Optional override — use ${DB_PASSWORD} placeholder or omit this line entirely
# MONGODB_URI=mongodb+srv://rickymach7:${DB_PASSWORD}@cluster0.jhjif7w.mongodb.net/fgcflow?appName=Cluster0

PORT=3001
CORS_ORIGIN=http://localhost:5173,https://rickyuxen.github.io
