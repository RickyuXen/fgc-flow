{
  "name": "fgc-flow",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"wait-on http://localhost:3001/api/games -t 30000 && npm run dev --prefix frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "deploy": "npm run build --prefix frontend && gh-pages -d frontend/dist"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^9.0.10"
  }
}
